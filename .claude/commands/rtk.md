Use the `rtk` skill for the current request.

If the request is about Redux Toolkit, follow the repository's `rtk` skill guidance and apply it to the user's task.

If the request is unrelated to Redux Toolkit, ignore this command and continue with the best available workflow.
